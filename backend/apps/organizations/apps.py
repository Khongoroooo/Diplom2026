from django.apps import AppConfig

class OrganizationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.organizations'  # Энэ хэсэг заавал 'apps.'-тайгаа байх ёстой